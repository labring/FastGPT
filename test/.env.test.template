# Test environment variables template.
# Copy to .env.test.local and fill in values. .env.test.local is git-ignored.
# 测试环境变量模板：复制为 .env.test.local 并填写实际值，.env.test.local 已加入 gitignore。

# MongoDB (used by globalSetup for most tests; in-memory repl set is auto-provided if not set)
# MONGODB_URI=

# ========== Vector DB integration tests (test/vectorDB) ==========
# Set the ones you need; unset vars cause that driver's integration tests to be skipped.

# PostgreSQL + pgvector connection string
# PG_URL=postgresql://user:password@localhost:5432/dbname

# Oceanbase (optional, for Oceanbase vector integration tests)
# OCEANBASE_URL=

# Milvus / Zilliz Cloud (optional)
# MILVUS_ADDRESS=
# MILVUS_TOKEN=

# SeekDB (optional)
# SEEKDB_URL=
