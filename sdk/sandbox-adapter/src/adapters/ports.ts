/**
 * OpenSandbox exposes execd through this direct endpoint. code-server itself is
 * still reached via execd path proxying at /proxy/8080.
 */
export const OPEN_SANDBOX_EXECD_PORT = 44772;
export const OPEN_SANDBOX_CODE_SERVER_PORT = 8080;

export const SEALOS_DEVBOX_CODE_SERVER_PORT = 1318;
