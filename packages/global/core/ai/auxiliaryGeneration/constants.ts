export enum AuxiliaryGenerationEventEnum {
  answer = 'answer',
  error = 'error',
  status = 'status',
  interactive = 'interactive',
  chatAgentConfig = 'chatAgentConfig'
}

export enum ChatAgentHelperTypeEnum {
  chatAgent = 'chatAgent'
}
