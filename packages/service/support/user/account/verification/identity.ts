/**
 * 账号身份验证实例的未来接入边界。
 *
 * 当前阶段仅提供 password、code、captcha、oauth、wechat 独立实例，
 * 不接入登录、注册、找回密码、改密、注销等生产业务。
 *
 * 后续接入原则：
 * 1. 业务层根据自身场景显式选择验证实例，不在本目录实现自动 resolver。
 * 2. 验证实例只负责校验现有凭据，并保留各自原有的返回结构。
 * 3. HTTP 适配层必须保留原请求解析、客户端 IP 提取和错误响应映射；实例不替它猜测上下文。
 * 4. 密码实例只返回凭据匹配用户；禁用状态、WeCom 限制等可登录性策略仍由登录层执行。
 * 5. 微信协议层只负责协议处理，通过 onMessage 由调用方注入场景语义和材料类型。
 * 6. 用户查询、建号、团队、Session、Cookie、审计和埋点仍由业务层负责。
 * 7. 每次只迁移一个现有入口，并通过成功、异常和副作用顺序测试证明逻辑不变。
 */
