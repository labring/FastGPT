import type { ChatCompletionTool } from '@fastgpt/global/core/ai/type';

export enum SubAppIds {
  plan = 'plan_agent',
  ask = 'ask_agent',
  stop = 'stop_agent',
  model = 'model_agent',
  fileRead = 'file_read'
}
