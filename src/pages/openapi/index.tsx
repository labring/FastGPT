import React from 'react';
import { Card, Box, Flex, Button, Input } from '@chakra-ui/react';

const OpenApi = () => {
  return (
    <Card px={6} py={4}>
      ss
    </Card>
  );
};

export default OpenApi;
