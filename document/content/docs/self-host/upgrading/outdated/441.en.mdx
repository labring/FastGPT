---
title: V4.4.1 (includes upgrade script)
description: Upgrade guide from older versions to FastGPT V4.4.1
---

## Run the Initialization API

Send 1 HTTP request (remember to include `headers.rootkey` — this value comes from your environment variables):

1. https://xxxxx/api/admin/initv441

```bash
curl --location --request POST 'https://{{host}}/api/admin/initv441' \
--header 'rootkey: {{rootkey}}' \
--header 'Content-Type: application/json'
```

This will initialize the `dataset.files` collection in MongoDB, marking all data as available.
