---
capability_label: 组织
doc_type: "11"
doc_label: 共享能力清单
generated_at: "2026-06-18T00:00:00.000Z"
parent_module: 团队管理
roles: [团队管理员, 团队成员]
router_paths: [/account/team?teamTab=org]
---

# 组织 — 共享能力清单

## 对外暴露的 Hook/API

| 导出项 | 类型 | 用途 | 使用者 | 使用场景 |
|--------|------|------|--------|---------|
| `useOrg` | Hook | 组织层级导航、子组织列表加载、成员分页查询、路径面包屑、搜索和刷新等核心状态管理 | 成员管理（MemberManager/MemberModal） | 在成员管理弹窗中选择组织时，需要复用组织导航和列表加载能力 |

## 跨模块依赖详情

### 被以下模块依赖 (imported_by)

| 消费模块 | 引用的具体内容 | 引用方式 | 首次引入 |
|---------|--------------|---------|---------|
| 权限-成员管理 | `useOrg` Hook — 组织选择和层级导航能力 | `import useOrg from '@/web/support/user/team/org/hooks/useOrg'` | — |

### 依赖以下模块 (imports_from)

| 依赖模块 | 使用的具体能力 | 引用方式 | 关键度 |
|---------|--------------|---------|--------|
| 用户Store | `useUserStore` — 用户信息、团队信息、管理员权限判断 | `import { useUserStore } from '@/web/support/user/useUserStore'` | 核心 |
| 系统配置 | `useSystemStore` — feConfigs 注册方式配置 | `import { useSystemStore } from '@/web/common/system/useSystemStore'` | 核心 |
| 团队上下文 | `TeamContext` — 团队编辑数据和团队规模 | `import { TeamContext } from '@/pageComponents/account/team/context'` | 可选 |
| 账户容器 | `AccountContainer` — 页面布局外壳 | `import AccountContainer from '@/pageComponents/account/AccountContainer'` | 核心 |
| Web公共Hooks | `useScrollPagination`、`useRequest`、`useConfirm` — 分页、请求管理、确认弹窗 | `import { useScrollPagination } from '@fastgpt/web/hooks/useScrollPagination'` 等 | 核心 |
| 公共组件库 | `MyModal`、`Avatar`、`MyIcon`、`SearchInput`、`FillRowTabs`、`MyMenu`、`Path` 等 | `import X from '@fastgpt/web/components/common/X'` | 核心 |
| 团队API | `getTeamMembers`、`delRemoveMember` — 团队成员查询和移除 | `import { getTeamMembers } from '@/web/support/user/team/api'` | 核心 |
