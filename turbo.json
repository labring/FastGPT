{
  "$schema": "https://turborepo.com/schema.json",
  "ui": "tui",
  "tasks": {
    "dev": {
      "cache": false,
      "persistent": true
    },
    "dev:pro": {
      "with": ["@fastgpt/admin#dev"],
      "cache": false,
      "persistent": true
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "dist/**", "worker/**"]
    },
    "lint": {
      "outputs": []
    },
    "test": {
      "passThroughEnv": ["FASTGPT_TEST_MONGODB_URI", "FASTGPT_TEST_MAX_WORKERS"],
      "outputs": ["coverage/**", "test-results.json"]
    },
    "test:integration": {
      "cache": false,
      "outputs": ["test-results.integration.json"]
    },
    "typecheck": {
      "outputs": []
    },
    "build:workers": {
      "cache": false,
      "outputs": ["worker/**"]
    }
  }
}
