'use client';
import AdminContainer from '@/pageComponents/admin/AdminContainer';
import CostPage from '@/pageComponents/admin/dashboard/cost';

const AdminPage = () => {
  return (
    <AdminContainer>
      <CostPage />
    </AdminContainer>
  );
};

export default AdminPage;
